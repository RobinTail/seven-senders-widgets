export const languageStep = 'languageStep';
export const nameStep = 'nameStep';
export const steps = [languageStep, nameStep];
export const firstStep = steps.slice().shift();
export const lastStep = steps.slice().pop();
